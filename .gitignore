node_modules/
dist/
build/

# Agent tooling. Contains nested git worktrees; `git add -A` would otherwise
# stage them as embedded git repositories, which is nasty to unpick later.
.claude/

# Never commit secrets. Visibility is a toggle; history outlives it.
.env
.env.*
!.env.example

# Generated simulation runs contain agent output nobody has read yet.
# Review before adding one deliberately.
runs/

*.log
.DS_Store
